using System.Linq;
using dotnet_portfolio_api.Models;

namespace dotnet_portfolio_api.Data
{
    //Ce fichier servira surtout à ajouter des données lors des tests
    public static class SeedData
    {
        public static void Initialize(PortfolioContext context)
        {
            // Données gérées manuellement via SQLite3
            // Pas de seed automatique
        }
    }
}
